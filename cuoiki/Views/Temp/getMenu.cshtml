@model IEnumerable<cuoiki.Models.MenuBar>
@foreach (var i in Model)
{
    <li class="nav-item active">
        <a class="nav-link" href="index.html">@i.name <span class="sr-only">(current)</span></a>
    </li>
}


